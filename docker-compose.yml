services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dualstyle
      POSTGRES_PASSWORD: dualstyle
      POSTGRES_DB: dualstyle
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
